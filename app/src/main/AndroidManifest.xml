<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelPlanner">
        <activity
            android:name=".JaipurPlacesActivity"
            android:exported="false" />
        <activity
            android:name=".ManaliPlacesActivity"
            android:exported="false" />
        <activity
            android:name=".DelhiPlacesActivity"
            android:exported="false" />
        <activity
            android:name=".GoaPlacesActivity"
            android:exported="false" /> <!-- New Activity (Place Details Screen) -->
        <activity
            android:name=".PlaceDetailsActivity"
            android:exported="false" /> <!-- NEW: Uttarakhand Places Screen -->
        <activity
            android:name=".UttarakhandPlacesActivity"
            android:exported="true" /> <!-- Old Details Screen -->
        <activity
            android:name=".DetailsActivity"
            android:exported="false" /> <!-- Launcher (Main Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>